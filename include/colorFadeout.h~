#ifndef COLORFADEOUT_H
#define COLORFADEOUT_H

#include <colorMod.h>

class colorFadeout: public colorMod
{
    public:
        colorFadeout();
        void update(char letter, unsigned int current, unsigned int length);
    protected:
    private:
};

#endif // COLORFADEOUT_H
