#ifndef COLORRED_H
#define COLORRED_H

#include <colorMod.h>

class colorRed: public colorMod
{
    public:
        colorRed();
        void update(char letter, unsigned int current, unsigned int length);
    protected:
    private:
};

#endif // COLORRED_H
