#ifndef COLORRAINBOW_H
#define COLORRAINBOW_H

#include <colorMod.h>

class colorRainbow: public colorMod
{
    public:
        colorRainbow();
        void update(char letter, unsigned int current, unsigned int length);
    protected:
    private:
};

#endif // COLORRAINBOW_H
